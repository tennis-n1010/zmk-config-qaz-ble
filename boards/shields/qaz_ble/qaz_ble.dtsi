#include <dt-bindings/zmk/matrix_transform.h>
#include "physical_layouts.dtsi"

/ {
    compatible = "qaz_ble,shield";

    chosen {
        zmk,kscan = &kscan0;
        zmk,physical-layout = &qaz_ble_wk_double_space_layout;
    };
};
